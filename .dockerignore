.git
.gitignore
.github

node_modules
dist
coverage
.nyc_output
.goals
.openwa
.env
.env.*
metrics
venv
infra/mosquitto/passwordfile

# Face recognition models: provisioned at runtime (entrypoint) into a volume,
# never part of the build context (774MB+ local, gitignored).
models
apps/backend/models

*.md
LICENSE

# Turborepo cache (never part of the build context)
.turbo/
apps/*/.turbo/
packages/*/.turbo/
node_modules/.cache/turbo

vitest.config.mts
nodemon.json
*.log
